server:
  forward-headers-strategy: framework
  shutdown: graceful

spring:
  application:
    name: request-service
  main:
    banner-mode: off
  resources:
    add-mappings: false
  mvc:
    throw-exception-if-no-handler-found: true

